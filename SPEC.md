# Fusion 360 OpenLOCK クリップ穴生成アドオン仕様（初版）

## 1. 目的

Fusion 360 のスケッチまたはソリッドモデル上で、Printable Scenery の OpenLOCK 互換クリップを受け入れるための穴形状を、指定した位置に作成できるようにする。

OpenLOCK は、タイルや壁などを標準化された接続部で組み合わせるモジュール式の 3D プリントシステムである。本アドオンが対象とする仕様バージョンは、Printable Scenery 公式の現行 OpenLOCK Clips パッケージ v5.5 に固定する。旧バージョン向けの寸法切り替えは行わない。

参照：<https://www.printablescenery.com/product/openlock-clips/>

## 2. 用語

| 用語 | 定義 |
|---|---|
| クリップ穴 | OpenLOCK クリップの爪・本体を挿入するための、対象部品側に設ける開口部 |
| 穴プロファイル | 正面から見たクリップ穴の 2D 輪郭。今回の添付図に相当する |
| 配置面 | 穴プロファイルをスケッチする平面。既存平面またはソリッドの平面面 |
| 基準線 | 編集中のスケッチから選ぶ直線。輪郭の位置と向きを決める基準 |
| スケッチ生成 | 穴プロファイルの閉じた輪郭を Fusion 360 のスケッチとして作成する処理 |
| 切り抜き | 穴プロファイルを押し出しカットし、対象ソリッドに貫通または指定深さの開口を作る処理 |

「スケッチの場所」は、初版では「配置面」と「基準線」の組み合わせとして扱う。点や座標の入力は求めない。

## 3. 対象ユーザーと利用シナリオ

### 必須シナリオ

1. ユーザーが Fusion 360 でアドインを起動し、対象スケッチの編集を開始する。
2. Sketch タブの OpenLOCK コマンドを実行する。
3. 編集中のスケッチから基準線を1本選択する。
4. 表示されるプレビューで向きを確認し、必要なら `Flip` で反対側へ切り替える。
5. 寸法図に基づく閉じた穴プロファイルが編集中のスケッチへ追加され、線長・角度を保つ拘束が設定される。
6. 13.80 mm上辺が基準線と同一直線に拘束された輪郭を確認し、寸法確認や押し出しカットを行う。

### 将来シナリオ

- ソリッドの面と配置点を選ぶと、その面に対して穴を自動で切り抜く。
- 同一面に複数個の穴を、間隔や個数を指定して配置する。
- 基本クリップ、スプリング、マグネット、グリップ、PassThru などを選択する。
- 3D プリントの嵌合調整用クリアランスを設定する。

## 4. 初版の機能範囲（MVP）

### 4.1 入力

- 対象バージョン：公式現行パッケージ `OpenLOCK Clips v5.5` のみ
- 穴タイプ：v5.5 パッケージ内の標準 `Basic OpenLOCK` クリップ用のみ
- 単位：Fusion 360 のドキュメント単位に従う。ただし寸法データは mm を正規値とする
- 実行条件：ユーザーが編集中のスケッチが存在すること
- 基準線：編集中のスケッチ線を必ず1本選択する。輪郭上辺（13.80 mm）の中点を初期配置で線の中点に合わせ、上辺を基準線と同一直線に拘束する
- 配置点・座標・回転角の入力：なし
- 反転：表裏反転のチェックボックス
- オフセット入力：なし
- プレビュー：基準線選択後に輪郭の向きを表示し、`Flip` の変更を反映する

### 4.2 出力

- 編集中のスケッチへ、クリップ穴の閉じた輪郭を 1 個追加する
- 線長を9種類の寸法と等長拘束で定義し、独立した15箇所の角度拘束と13.80 mm上辺の同一直線拘束を追加する。頂点の固定拘束は使わず、上辺に沿った移動を許す
- 追加した輪郭の各線にプロファイルID・配置情報を属性として保存する
- 参照線との同一直線関係をスケッチ拘束として維持し、参照線の編集にも追従させる
- コマンドは Design ワークスペースの Sketch コンテキストにのみ表示する
- MVP ではソリッドの切り抜きは自動実行しない

### 4.3 エラー条件

- 編集中のスケッチが存在しない
- 対象が読み取り専用・編集不能である
- プロファイル作成に失敗した場合は、追加途中の線を残さない

## 5. 穴プロファイル寸法（2026-09-19 寸法図の読み取り）

下表を、ユーザー提供の寸法入りスクリーンショットに基づく実装値とする。メーカー公式図面や元 CAD/STL との照合は未実施のため、OpenLOCK v5.5 の公式寸法としては扱わない。輪郭は左右対称で、幅広の外形側を上にし、中央スリットは下辺から上向きに開く。座標原点は下辺中央とする。

| 寸法 | 値 | 輪郭上の意味 |
|---|---:|---|
| 全高 | 9.80 mm | 下辺から上辺まで |
| 上辺の最大幅 | 13.80 mm | 中心線から左右端まで各6.90 mm |
| 上側段差の高さ | 2.00 mm | 上辺から外側の水平段まで |
| 上側段差の幅 | 1.00 mm | 外端から内側段まで、左右各々 |
| 上辺から中段まで | 5.50 mm | 中段の高さは下辺基準で4.30 mm |
| 中段の内端位置 | 4.76 mm | 中心線から棚の内端まで。値は固定 |
| 中段の水平幅 | 1.00 mm | x=4.76 mmからx=5.76 mmまで |
| 下側の段差高さ | 2.00 mm | 下辺から下側斜面の終点まで |
| スリット壁から斜面開始点まで | 2.97 mm | 下辺の水平部。開始点x=3.77 mm |
| 中央スリット幅 | 1.60 mm | 全幅。図中の0.80 mmは中心線から片側壁まで |
| 中央スリットの深さ | 7.20 mm | 下辺から上向き |
| 下側斜面の角度 | 135.0° | (3.77, 0) から (5.76, 2.00) へ接続 |

中段は y=9.80-5.50=4.30 mm に置く。上側斜線は (4.76, 4.30) と (5.90, 7.80) を結ぶ。下側斜線はスリット壁から2.97 mm離れた x=3.77 mm を始点とし、x=5.76 mm、y=2.00 mm の下側段差へ接続する。寸法間の丸め差を残し、4.76 mmの基準位置は変更しない。

### 寸法確定時のルール

- 対称形状として定義できる部分は、左右を別々の固定線分にせず中心線から拘束する。
- 寸法はハードコードせず、クリップタイプ定義として保持する。
- 角度の基準は、Fusion 360 のスケッチ拘束で再現できる形に変換する。
- 公式寸法との照合後、採用元とバージョンを仕様書に記録する。

## 6. 推奨 UI

コマンド名：`Create OpenLOCK Hole`

ダイアログ項目：

1. `Clip Type`：Basic OpenLOCK v5.5（MVP では固定）
- `Reference Edge`：必須。スケッチ線を1本選ぶと、13.80 mm上辺の中点をその線の中点に合わせる
- プレビュー：作成前に輪郭の位置・向きを表示する
- `Flip`：基準線を軸に輪郭を反対側へ反転

配置点、座標、回転角を入力する UI と `Placement Mode` は設けない。

## 7. 将来のソリッド切り抜き仕様

### 入力

- 対象ソリッドの平面面
- 面上の配置点
- 穴の向き
- カット深さ：貫通、指定深さ、面からの距離
- クリアランス

### 処理

1. 選択面にスケッチを作成する。
2. 穴プロファイルを配置する。
3. プロファイルを面の法線方向に押し出す。
4. `Cut` のブーリアン演算を実行する。
5. 元スケッチを残すか、非表示にするかを設定する。

### 注意点

- 曲面への配置は初版対象外とする。
- 複数ボディにまたがるカットは初版対象外とする。
- 面の向きによって穴が外側へ押し出されないよう、法線方向を確認する。
- 薄い壁ではクリアランスにより開口がつながる可能性があるため、警告を出せるとよい。

## 8. 非機能要件

- Fusion 360 の標準的なアドインとしてインストール・起動できること
- コマンドのキャンセルで中途半端なスケッチを残さないこと
- 生成後に通常の Fusion 360 スケッチとして編集できること
- 寸法定義をコードと UI から分離し、将来の OpenLOCK バージョン追加を容易にすること
- 商用利用や再配布を行う場合は、OpenLOCK のライセンス条件を別途確認すること

## 9. 受け入れ条件

- スケッチ編集中に Sketch タブから実行すると、編集中のスケッチへ閉じた輪郭が追加される。
- スケッチ編集中でないときは、コマンドが表示されず実行もできない。
- 基準線を選択すると、13.80 mm上辺の中点が初期配置で選択線の中点に一致し、上辺が選択線と同一直線に拘束される。
- 基準線を選択すると、確定前に向きのプレビューが表示される。
- プレビューで `Flip` を切り替えると、輪郭が選択線の反対側へ移る。
- 作成後も13.80 mm上辺と選択線の同一直線関係が維持される。
- Move OpenLOCK Hole コマンドや垂直オフセット入力を表示しない。
- 既存のスケッチ図形を変更せず、輪郭だけが追加される。
- 寸法の検証値が確定後、添付図または公式リファレンスと一致する。

## 10. 未決事項

- 寸法の単位（画像は mm と推定）
- 寸法図の読み取りが元 CAD/STL の形状と一致するか
- 135° が内角か外角か
- v5.5 の Basic クリップ用プロファイル寸法を公式資料で確定する
- v5.5 パッケージに含まれる Springy / Magnetic / Grip / PassThru を将来対象に含めるか
- 穴を「輪郭そのもの」にするか、クリップ挿入時のクリアランスを含めるか
- 複数のOpenLOCK輪郭を一括選択・移動するUIをアドイン側で追加するか
- 曲面、複数ボディ、薄肉形状をサポートするか
